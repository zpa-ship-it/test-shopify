<section class="page-shell">
  <div class="page-hero">
    <p class="eyebrow">{{ shop.name }}</p>
    <h1>{{ page.title }}</h1>
  </div>
  <div class="rte glass-panel">{{ page.content }}</div>
</section>

{% schema %}
{
  "name": "Page"
}
{% endschema %}
